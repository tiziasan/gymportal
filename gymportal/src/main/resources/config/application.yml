server:
  servlet:
    context-path: /gymportal

spring:
  application:
    name: GymPortal
  profiles:
    active: dev
  #jackson:
    #default-property-inclusion: NON_NULL
    #serialization:
      #write-dates-as-timestamps: true
  messages:
    basename: i18n/common, i18n/gymportal
    encoding: UTF-8
    use-code-as-default-message: true
    
###############################################
############# GYMPORTAL PROPERTIES #############
###############################################

gymportal:
  dateformat: dd/MM/yyyy